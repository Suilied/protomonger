#include "BattleManager.h"

void BattleManager::PrepareBattlefieldData() {
	// gather participant data
	// gather environment data
	// set-up initial positions (and directions) for participants
}

void BattleManager::SetupTestData() {
	Army* army1 = new Army();

	_armies.push_back(army1);
}